<Window
    x:Class="FindAncestor.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:FindAncestor"
    Title="Home"
    MinWidth="1600"
    MinHeight="800"
    WindowStartupLocation="CenterScreen">

    <Grid>

        <!--  戻るボタン  -->
        <Button
            Width="100"
            Height="40"
            Margin="10"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Command="{Binding BackCommand}"
            Content="戻る" />

        <Grid Margin="0,60,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <local:ImageDisplayView Grid.Column="0" DataContext="{Binding ParentA}" />

            <local:ParentBView Grid.Column="1" DataContext="{Binding ParentB}" />

            <local:ParentCView Grid.Column="2" DataContext="{Binding ParentC}" />

            <local:ParentDView Grid.Column="3" DataContext="{Binding ParentD}" />
        </Grid>

    </Grid>
</Window>